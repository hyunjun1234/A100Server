# CudaForge (OptimAI-Lab/CudaForge). 우선 공식 repo CLI 사용을 시도하고,
# endpoint 주입이 안 되면 inline 재구현으로 전환.
# [repo CLI 확인 후 택1 — 기본은 inline]
#SYSTEM_CWD='third_party/CudaForge'
#SYSTEM_CMD='python3 main.py {TASK} --server_type vllm --server_address ${SERVER_HOST} --server_port ${SERVER_PORT} --model_name ${MODEL_ALIAS} --device ${DEVICE_ID} --round {ROUNDS} --work_dir {CAND_DIR} --temperature {TEMP} --top_p ${TOP_P} --max_tokens ${MAX_NEW_TOKENS}'
#CANDIDATE_GLOB='**/round*_kernel.py'   # repo 출력 규약 확인 후 조정
SYSTEM_CMD='python3 drivers/cudaforge_inline.py --task {TASK} --cand_dir {CAND_DIR} --rounds {ROUNDS} --seed {SEED} --temperature {TEMP}'
CANDIDATE_GLOB='round*_kernel.py'
